# -----------------------------------------------------------
# Zitadel — Identity Provider (OIDC/OAuth)
# Chart: zitadel/zitadel
# Console: https://auth.acdgbrasil.com.br/ui/console
# HelmRepository: clusters/master-xeon/zitadel-repo.yaml
# -----------------------------------------------------------

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zitadel
spec:
  interval: 5m
  chart:
    spec:
      chart: zitadel
      sourceRef:
        kind: HelmRepository
        name: zitadel
        namespace: flux-system
  values:
    replicaCount: 1

    zitadel:
      masterkeySecretName: zitadel-masterkey
      configSecretName: zitadel-db-credentials
      configSecretKey: config.yaml
      configmapConfig:
        ExternalDomain: "auth.acdgbrasil.com.br"
        ExternalSecure: true
        ExternalPort: 443
        TLS:
          Enabled: false
        Database:
          Postgres:
            Host: "postgres"
            Port: 5432
            Database: "zitadel"
            User:
              Username: "zitadel"
              SSL:
                Mode: "disable"
            Admin:
              Username: "postgres"
              SSL:
                Mode: "disable"
        FirstInstance:
          Org:
            Human:
              UserName: "admin"
              FirstName: "ACDG"
              LastName: "Admin"
              Email: "admin@acdgbrasil.com.br"
              Password: "AcdgAdmin2024!"
              PasswordChangeRequired: true

    ingress:
      enabled: true
      className: traefik
      annotations:
        kubernetes.io/ingress.class: traefik
      hosts:
        - host: auth.acdgbrasil.com.br
          paths:
            - path: /
              pathType: Prefix
      tls: []

    login:
      ingress:
        enabled: true
        className: traefik
        annotations:
          kubernetes.io/ingress.class: traefik
        hosts:
          - host: auth.acdgbrasil.com.br
            paths:
              - path: /ui/v2/login
                pathType: Prefix
        tls: []

    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi

    nodeSelector:
      hardware-type: high-performance
